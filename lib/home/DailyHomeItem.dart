import 'package:flutter/widgets.dart';

class DailyHomeItem extends StatelessWidget {
  final int index;
  const DailyHomeItem({ Key? key, required this.index }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(

    );
  }
}